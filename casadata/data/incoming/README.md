# data/incoming — datasets à ingérer

Déposer ici les CSV historiques avec leur manifest de provenance
(`fichier.csv.manifest.json` — voir `docs/OPERATIONS.md`).

Rien dans `data/` n'est versionné (voir .gitignore) : la base, le brut et les
exports restent locaux.
